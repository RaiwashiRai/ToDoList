<!Doctype html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>To Do List</title>
    </head>
    <body>
        <!-- visit ejs documentation for more info on tags -->
        <!-- <%if (kindOfDay === "Wednesday"){%>
            <h1 style="color : violet">It's a <%= kindOfDay %> To Do List </h1>
        <%} else{%>
            <h1 style="color: purple">It's a <%= kindOfDay %> To Do List </h1>
        <%}%> -->

        <h1> <%= kindOfDay %> </h1>

        <ul>
            <% for (var i = 0; i < newListItem.length; i++){%>
                <li> <%= newListItem[i] %> </li>
            <%}%>
        </ul>

        <form class="" action="/" method="post">
            <input type="text" name="newListItem_" value="">
            <button type="submit" name="button">Add</button>
        </form>

    </body>
</html>
